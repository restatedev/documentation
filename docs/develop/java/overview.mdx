---
sidebar_position: 1
description: "Get an idea of what a Restate Java service looks like."
---

import Admonition from '@theme/Admonition';
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

# Overview

The Restate Java/Kotlin SDK is open source and can be found on GitHub: ([sdk-java repo](https://github.com/restatedev/sdk-java)).

## Installation

<Admonition type="tip">
Have a look at the [Java Quickstart](/get_started/quickstart?sdk=jvm)!
</Admonition>

You can use the build tool of your choice with the Java/Kotlin SDK. The following instructions use Gradle (Kotlin script).

<Tabs groupId="sdk" queryString>
<TabItem value="java" label="Java">

Run:

```shell
gradle init --type java-application
```


Add the runtime dependency `sdk-api` and the annotation processor dependency `sdk-api-gen`:

```kotlin
annotationProcessor("dev.restate:sdk-api-gen:VAR::JAVA_SDK_VERSION")
implementation("dev.restate:sdk-api:VAR::JAVA_SDK_VERSION")
```

When using composite types/POJOs for input/output and state, [Jackson Databind](https://github.com/FasterXML/jackson) will be used by default.
The Jackson dependency is not automatically included, you must add it with `sdk-serde-jackson`:

```kotlin
implementation("dev.restate:sdk-serde-jackson:VAR::JAVA_SDK_VERSION")
```

</TabItem>
<TabItem value="kotlin" label="Kotlin">

Run:

```shell
gradle init --type kotlin-application
```

Add the [Kotlin symbol processing](https://kotlinlang.org/docs/ksp-quickstart.html#use-your-own-processor-in-a-project) and the [Kotlin serialization](https://kotlinlang.org/docs/serialization.html) plugin:

```kotlin
plugins {
    kotlin("plugin.serialization") version "1.9.22"
    id("com.google.devtools.ksp") version "1.9.22-1.0.18"
}
```

Add the runtime dependency `sdk-api-kotlin` and the `ksp` dependency `sdk-api-kotlin-gen`:

```kotlin
ksp("dev.restate:sdk-api-kotlin-gen:VAR::JAVA_SDK_VERSION")
implementation("dev.restate:sdk-api-kotlin:VAR::JAVA_SDK_VERSION")
```

</TabItem>
</Tabs>

Depending on the deployment target, add one of the following dependencies:

- To run as long-running app: `dev.restate:sdk-http-vertx:VAR::JAVA_SDK_VERSION`
- To run on AWS Lambda: `dev.restate:sdk-lambda:VAR::JAVA_SDK_VERSION`

## API Examples

Below is an example of a virtual object.

The Java/Kotlin SDK uses annotations to define the component and its handlers.
The example shows a greeter service with a single handler `greet`, that maintains a record of the number of times it has received a request for a given name, and prints a greeting.

<Tabs groupId="sdk" queryString>
<TabItem value="java" label="Java">

```java
CODE_LOAD::java/src/main/java/develop/Greeter.java
```

</TabItem>
<TabItem value="kotlin" label="Kotlin">

```kotlin
CODE_LOAD::kotlin/src/main/kotlin/develop/Greeter.kt
```

</TabItem>
</Tabs>

Now that you have a high-level idea of what a Restate service might look like, let's dive into the details!
